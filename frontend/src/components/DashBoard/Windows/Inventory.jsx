import React from "react";

export const Inventory = ({ userDetails }) => {
  const user = userDetails;
  return (
    <div className="p-5">
      <h1 className="text-3xl font-semibold text-center">
        INVENTORY MANAGEMENT
      </h1>
      
    </div>
  );
};
