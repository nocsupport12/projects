import React from "react";

export const Sales = ({ userDetails }) => {
  const user = userDetails;
  return (
    <div className="p-5">
      <h1 className="text-3xl font-semibold text-center">SALES</h1>
    </div>
  );
};
